# NOTE: This file is inside backend/ and is NOT auto-loaded by Gunicorn.
# Gunicorn looks for gunicorn.conf.py in the CWD (repo root), not here.
# All active Gunicorn configuration lives at: /gunicorn.conf.py (repo root).
# This file is kept for reference only.
